# 农田档案数据库数据字典

> 生成时间: 2026-03-18
> 版本: v1.0
> 数据库: farmland_test

---

## 目录

1. [组织与权限模块](#1-组织与权限模块)
2. [基地与地块模块](#2-基地与地块模块)
3. [作物与生长模块](#3-作物与生长模块)
4. [传感器与监测模块](#4-传感器与监测模块)
5. [气象数据模块](#5-气象数据模块)
6. [遥感影像模块](#6-遥感影像模块)
7. [土壤检测模块](#7-土壤检测模块)
8. [预警模块](#8-预警模块)
9. [报表模块](#9-报表模块)
10. [农事活动模块](#10-农事活动模块)
11. [病虫害模块](#11-病虫害模块)
12. [智能体模块](#12-智能体模块)

---

## 1. 组织与权限模块

### 1.1 organization - 组织机构表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| name | varchar(100) | NO | | NULL | 组织名称 |
| status | tinyint | YES | | 1 | 状态: 1启用, 0禁用 |
| phone | varchar(20) | YES | | NULL | 联系电话 |
| created_at | datetime | YES | | CURRENT_TIMESTAMP | 创建时间 |

**索引信息**: 1个 (主键)

---

### 1.2 user - 用户表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| user_id | bigint | NO | UNI | NULL | 用户唯一标识 |
| user_name | varchar(100) | NO | | NULL | 用户名 |
| phone | varchar(20) | YES | | NULL | 手机号 |
| avatar_url | varchar(500) | YES | | NULL | 头像URL |
| gender | tinyint | YES | | NULL | 性别: 0未知, 1男, 2女 |
| name | varchar(100) | YES | | NULL | 真实姓名 |
| password | varchar(255) | YES | | NULL | 密码(加密) |
| status | tinyint | YES | | 1 | 状态: 1启用, 0禁用 |
| created_at | datetime | YES | | CURRENT_TIMESTAMP | 创建时间 |

**索引信息**: 2个 (主键, user_id唯一)

---

### 1.3 role - 角色表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| code | varchar(50) | YES | | NULL | 角色代码 |
| role_name | varchar(100) | NO | | NULL | 角色名称 |
| status | tinyint | YES | | 1 | 状态: 1启用, 0禁用 |
| created_at | datetime | YES | | CURRENT_TIMESTAMP | 创建时间 |

**索引信息**: 1个 (主键)

---

### 1.4 permission - 权限表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| code | varchar(100) | YES | | NULL | 权限代码 |
| name | varchar(100) | NO | | NULL | 权限名称 |
| status | tinyint | YES | | 1 | 状态: 1启用, 0禁用 |
| created_at | datetime | YES | | CURRENT_TIMESTAMP | 创建时间 |

**索引信息**: 1个 (主键)

---

### 1.5 user_organization - 用户-组织关联表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| user_id | bigint | NO | MUL | NULL | 用户ID (外键→user.id) |
| org_id | bigint | NO | MUL | NULL | 组织ID (外键→organization.id) |

**索引信息**: 3个 (主键, user_id, org_id)

**外键关系**:
- user_id → user.id
- org_id → organization.id

---

### 1.6 user_role - 用户-角色关联表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| user_id | bigint | NO | MUL | NULL | 用户ID (外键→user.id) |
| role_id | bigint | NO | MUL | NULL | 角色ID (外键→role.id) |
| is_primary | tinyint | YES | | 0 | 是否主要角色: 1是, 0否 |

**索引信息**: 3个 (主键, user_id, role_id)

**外键关系**:
- user_id → user.id
- role_id → role.id

---

### 1.7 role_permission - 角色-权限关联表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| role_id | bigint | NO | MUL | NULL | 角色ID (外键→role.id) |
| permission_id | bigint | NO | MUL | NULL | 权限ID (外键→permission.id) |

**索引信息**: 3个 (主键, role_id, permission_id)

**外键关系**:
- role_id → role.id
- permission_id → permission.id

---

### 1.8 agent_farmer_service - 智能体-农户服务表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| agent_id | bigint | NO | MUL | NULL | 智能体ID (外键→user.id) |
| farmer_id | bigint | NO | MUL | NULL | 农户ID (外键→user.id) |

**索引信息**: 3个 (主键, agent_id, farmer_id)

**外键关系**:
- agent_id → user.id
- farmer_id → user.id

---

## 2. 基地与地块模块

### 2.1 base - 基地表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| name | varchar(100) | NO | | NULL | 基地名称 |
| address | varchar(255) | YES | | NULL | 基地地址 |
| map | varchar(500) | YES | | NULL | 地图坐标/URL |
| status | tinyint | YES | | 1 | 状态: 1启用, 0禁用 |
| created_at | datetime | YES | | CURRENT_TIMESTAMP | 创建时间 |

**索引信息**: 1个 (主键)

---

### 2.2 plot - 地块表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| name | varchar(100) | NO | | NULL | 地块名称 |
| area_mu | decimal(10,2) | YES | | NULL | 面积(亩) |
| address | varchar(255) | YES | | NULL | 地块地址 |
| map | varchar(500) | YES | | NULL | 地图坐标/URL |
| soil_capacity | decimal(6,3) | YES | | NULL | 土壤容量(m³) |
| status | tinyint | YES | | 1 | 状态: 1启用, 0禁用 |
| created_at | datetime | YES | | CURRENT_TIMESTAMP | 创建时间 |

**索引信息**: 1个 (主键)

---

### 2.3 base_plot - 基地-地块关联表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| base_id | bigint | NO | MUL | NULL | 基地ID (外键→base.id) |
| plot_id | bigint | NO | MUL | NULL | 地块ID (外键→plot.id) |

**索引信息**: 3个 (主键, base_id, plot_id)

**外键关系**:
- base_id → base.id
- plot_id → plot.id

---

### 2.4 base_farmer - 基地-农户关联表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| base_id | bigint | NO | MUL | NULL | 基地ID (外键→base.id) |
| farmer_id | bigint | NO | MUL | NULL | 农户ID (外键→user.id) |

**索引信息**: 3个 (主键, base_id, farmer_id)

**外键关系**:
- base_id → base.id
- farmer_id → user.id

---

### 2.5 base_agent - 基地智能体表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| base_id | bigint | NO | MUL | NULL | 基地ID (外键→base.id) |
| agent_id | bigint | NO | MUL | NULL | 智能体ID (外键→user.id) |

**索引信息**: 3个 (主键, base_id, agent_id)

**外键关系**:
- base_id → base.id
- agent_id → user.id

---

## 3. 作物与生长模块

### 3.1 crop - 作物表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| name | varchar(100) | NO | | NULL | 作物名称 |

**索引信息**: 1个 (主键)

---

### 3.2 growing_season - 生长季表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| name | varchar(100) | NO | | NULL | 生长季名称 |
| crop_name | varchar(100) | YES | | NULL | 具体作物名称 |
| start_time | datetime | YES | | NULL | 开始时间 |
| end_time | datetime | YES | | NULL | 结束时间 |
| status | tinyint | YES | | 1 | 状态: 1启用, 0禁用 |
| created_at | datetime | YES | | CURRENT_TIMESTAMP | 创建时间 |

**索引信息**: 1个 (主键)

---

### 3.3 growing_season_crop - 生长季-作物关联表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| growing_season_id | bigint | NO | MUL | NULL | 生长季ID (外键→growing_season.id) |
| crop_id | bigint | NO | MUL | NULL | 作物ID (外键→crop.id) |

**索引信息**: 3个 (主键, growing_season_id, crop_id)

**外键关系**:
- growing_season_id → growing_season.id
- crop_id → crop.id

---

### 3.4 growing_season_plot - 生长季-地块关联表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| growing_season_id | bigint | NO | MUL | NULL | 生长季ID (外键→growing_season.id) |
| plot_id | bigint | NO | MUL | NULL | 地块ID (外键→plot.id) |

**索引信息**: 3个 (主键, growing_season_id, plot_id)

**外键关系**:
- growing_season_id → growing_season.id
- plot_id → plot.id

---

### 3.5 growing_season_growth_stage - 生长季-生长阶段关联表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| growing_season_id | bigint | NO | MUL | NULL | 生长季ID (外键→growing_season.id) |
| growth_stage_id | bigint | NO | MUL | NULL | 生长阶段ID (外键→growth_stage.id) |
| start_time | datetime | YES | | NULL | 开始时间 |
| end_time | datetime | YES | | NULL | 结束时间 |
| status | tinyint | YES | | 1 | 状态: 1启用, 0禁用 |

**索引信息**: 3个 (主键, growing_season_id, growth_stage_id)

**外键关系**:
- growing_season_id → growing_season.id
- growth_stage_id → growth_stage.id

---

### 3.6 growth_stage - 生长阶段字典表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| name | varchar(100) | NO | | NULL | 生长阶段名称 |

**索引信息**: 1个 (主键)

---

## 4. 传感器与监测模块

### 4.1 sensor - 传感器表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| name | varchar(100) | YES | | NULL | 传感器名称 |
| sensor_id | bigint | NO | | NULL | 传感器设备编号 |
| sensor_model | varchar(100) | YES | | NULL | 传感器型号 |
| status | tinyint | YES | | 1 | 状态: 1启用, 0禁用 |

**索引信息**: 1个 (主键)

---

### 4.2 sensor_type - 传感器类型字典表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| name | varchar(100) | NO | | NULL | 传感器类型名称 |

**索引信息**: 1个 (主键)

---

### 4.3 sensor_plot - 传感器-地块关联表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| sensor_id | bigint | NO | MUL | NULL | 传感器ID (外键→sensor.id) |
| plot_id | bigint | NO | MUL | NULL | 地块ID (外键→plot.id) |
| status | tinyint | YES | | 1 | 状态: 1启用, 0禁用 |

**索引信息**: 3个 (主键, sensor_id, plot_id)

**外键关系**:
- sensor_id → sensor.id
- plot_id → plot.id

---

## 5. 气象数据模块

### 5.1 weather_daily - 日气象数据表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| plot_id | bigint | NO | MUL | NULL | 地块ID (外键→plot.id) |
| time | datetime | NO | | NULL | 日期时间 |
| t2m_max | decimal(5,2) | YES | | NULL | 日最高温度 (°C) |
| t2x_min | decimal(5,2) | YES | | NULL | 日最低温度 (°C) |
| tp_sum | decimal(6,2) | YES | | NULL | 日降水量 (mm) |
| rh2m_avg | decimal(5,2) | YES | | NULL | 日平均相对湿度 (%) |
| ws10m_max | decimal(5,2) | YES | | NULL | 日最大风速 (m/s) |
| query_date | datetime | YES | | NULL | 查询日期 |

**索引信息**: 4个 (主键, plot_id, plot_id+time, plot_id+query_date)

**外键关系**:
- plot_id → plot.id

---

### 5.2 weather_hour - 小时气象数据表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| plot_id | bigint | NO | MUL | NULL | 地块ID (外键→plot.id) |
| time | datetime | NO | | NULL | 时间 |
| t2m | decimal | YES | | NULL | 温度 (°C) |
| tp | decimal | YES | | NULL | 降水量 (mm) |
| ws10m | decimal | YES | | NULL | 风速 (m/s) |
| rh2m | decimal | YES | | NULL | 相对湿度 (%) |
| query_date | datetime | YES | | NULL | 查询日期 |
| ssrd | decimal | YES | | NULL | 太阳辐射 (W/m²) |

**索引信息**: 4个 (主键, plot_id, plot_id+time, plot_id+query_date)

**外键关系**:
- plot_id → plot.id

---

## 6. 遥感影像模块

### 6.1 remote_sensing_image - 遥感影像表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| plot_id | bigint | NO | MUL | NULL | 地块ID (外键→plot.id) |
| time | datetime | NO | | NULL | 影像时间 |
| type | tinyint | YES | | NULL | 影像类型 |
| image | varchar(500) | YES | | NULL | 影像URL/路径 |

**索引信息**: 4个 (主键, plot_id, plot_id+time, plot_id+type)

**外键关系**:
- plot_id → plot.id

---

## 7. 土壤检测模块

### 7.1 soil_test - 土壤检测表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| plot_id | bigint | NO | MUL | NULL | 地块ID (外键→plot.id) |
| templates_id | bigint | YES | MUL | NULL | 模板ID (外键→soil_test_templates.id) |
| time | datetime | NO | | NULL | 检测时间 |
| data | json | YES | | NULL | 检测数据(JSON格式) |

**索引信息**: 5个 (主键, plot_id, templates_id, plot_id+time, plot_id+templates_id)

**外键关系**:
- plot_id → plot.id
- templates_id → soil_test_templates.id

---

### 7.2 soil_test_templates - 土壤检测模板表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| name | varchar(100) | NO | | NULL | 模板名称 |
| description | text | YES | | NULL | 模板描述 |

**索引信息**: 1个 (主键)

---

### 7.3 soil_test_items - 土壤检测项目字典表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| name | varchar(100) | NO | | NULL | 检测项目名称 |
| unit | varchar(20) | YES | | NULL | 单位 |

**索引信息**: 1个 (主键)

---

### 7.4 soil_items_templates - 土壤检测项目-模板关联表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| template_id | bigint | NO | MUL | NULL | 模板ID (外键→soil_test_templates.id) |
| item_id | bigint | NO | MUL | NULL | 检测项目ID (外键→soil_test_items.id) |

**索引信息**: 3个 (主键, template_id, item_id)

**外键关系**:
- template_id → soil_test_templates.id
- item_id → soil_test_items.id

---

## 8. 预警模块

### 8.1 alert - 预警记录表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| plot_id | bigint | NO | MUL | NULL | 地块ID (外键→plot.id) |
| time | datetime | NO | | NULL | 预警时间 |
| level_id | bigint | NO | MUL | NULL | 预警级别ID (外键→alert_level.id) |
| type_id | bigint | NO | MUL | NULL | 预警类型ID (外键→alert_type.id) |
| content | json | YES | | NULL | 预警内容(JSON格式) |
| status | tinyint | YES | | 1 | 状态: 1待处理, 0已处理 |

**索引信息**: 8个 (主键, plot_id, level_id, type_id, plot_id+time, plot_id+status, level_id+status, type_id+status)

**外键关系**:
- plot_id → plot.id
- level_id → alert_level.id
- type_id → alert_type.id

---

### 8.2 alert_level - 预警级别字典表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| name | varchar(100) | NO | | NULL | 预警级别名称 |

**索引信息**: 1个 (主键)

---

### 8.3 alert_type - 预警类型字典表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| name | varchar(100) | NO | | NULL | 预警类型名称 |

**索引信息**: 1个 (主键)

---

## 9. 报表模块

### 9.1 report - 报表表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| plot_id | bigint | NO | MUL | NULL | 地块ID (外键→plot.id) |
| time | datetime | NO | | NULL | 报表时间 |
| type_id | bigint | NO | MUL | NULL | 报表类型ID (外键→report_type.id) |
| original_data | json | YES | | NULL | 原始数据(JSON格式) |
| analysis | json | YES | | NULL | 分析结果(JSON格式) |
| advice | json | YES | | NULL | 建议(JSON格式) |

**索引信息**: 5个 (主键, plot_id, type_id, plot_id+time, plot_id+type_id)

**外键关系**:
- plot_id → plot.id
- type_id → report_type.id

---

### 9.2 report_type - 报表类型字典表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| name | varchar(100) | NO | | NULL | 报表类型名称 |

**索引信息**: 1个 (主键)

---

## 10. 农事活动模块

### 10.1 farm_activity - 农事活动表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| plot_id | bigint | NO | MUL | NULL | 地块ID (外键→plot.id) |
| time | datetime | NO | | NULL | 活动时间 |
| type_id | bigint | NO | MUL | NULL | 活动类型ID (外键→activity_type.id) |
| topic | varchar(200) | YES | | NULL | 活动主题 |
| basis | json | YES | | NULL | 活动依据(JSON格式) |
| content | json | YES | | NULL | 活动内容(JSON格式) |

**索引信息**: 5个 (主键, plot_id, type_id, plot_id+time, plot_id+type_id)

**外键关系**:
- plot_id → plot.id
- type_id → activity_type.id

---

### 10.2 activity_type - 活动类型字典表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| name | varchar(100) | NO | | NULL | 活动类型名称 |

**索引信息**: 1个 (主键)

---

## 11. 病虫害模块

### 11.1 pest_disease_type - 病虫害类型字典表

| 字段名 | 数据类型 | 允许空 | 键型 | 默认值 | 说明 |
|--------|----------|--------|------|--------|------|
| id | bigint | NO | PRI | NULL | 主键ID |
| name | varchar(100) | NO | | NULL | 病虫害名称 |
| type | tinyint | NO | | NULL | 类型: 1虫害, 2病害 |

**索引信息**: 1个 (主键)

---

## 附录：数据类型说明

### A. 气象数据字段

| 字段代码 | 含义 | 单位 |
|----------|------|------|
| t2m | 温度 | °C |
| t2m_max | 日最高温度 | °C |
| t2x_min | 日最低温度 | °C |
| tp | 降水量 | mm |
| tp_sum | 日降水量 | mm |
| rh2m | 相对湿度 | % |
| rh2m_avg | 日平均相对湿度 | % |
| ws10m | 风速 | m/s |
| ws10m_max | 日最大风速 | m/s |
| ssrd | 太阳辐射 | W/m² |

### B. 状态字段说明

| 值 | 含义 |
|----|------|
| status=1 | 启用/正常 |
| status=0 | 禁用/停用 |
| status=1 | 待处理 |
| status=0 | 已处理 |

### C. JSON字段说明

| 表名 | 字段名 | 内容示例 |
|------|--------|----------|
| alert | content | {"title": "温度预警", "value": 35, "unit": "°C"} |
| report | original_data | {"raw_data": [...]} |
| report | analysis | {"trends": [...], "patterns": [...]} |
| report | advice | {"recommendations": [...]} |
| soil_test | data | {"pH": 6.5, "nitrogen": "30mg/kg", ...} |
| farm_activity | basis | {"reason": "根据气象数据...", "reference": "..."} |
| farm_activity | content | {"tasks": [...], "details": "..."} |
