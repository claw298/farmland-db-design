# 农田档案数据库 - 版本1数据字典

**版本**: v1.0
**生成时间**: 2026-03-18 18:45
**状态**: Phase 1 完成
**表数量**: 37
**字符集**: utf8mb4

---

## 📊 验证结果

| 检查项 | 结果 |
|--------|------|
| 表总数 | ✅ 37 |
| 主键 | ✅ 每表1个 |
| 字段注释 | ✅ 全部有COMMENT |
| UNIQUE约束 | ✅ user.user_id |
| 导入测试 | ✅ 通过 |

---

## 📁 表分类

### 用户与权限 (7表)
| 表名 | 说明 |
|------|------|
| user | 用户表 |
| organization | 组织表 |
| role | 角色表 |
| permission | 权限表 |
| user_organization | 用户-组织关联 |
| user_role | 用户-角色关联 |
| role_permission | 角色-权限关联 |

### 基地与地块 (5表)
| 表名 | 说明 |
|------|------|
| base | 基地表 |
| plot | 地块表 |
| base_plot | 基地-地块关联 |
| base_farmer | 基地-农户关联 |
| base_agent | 基地-服务商关联 |

### 作物与生长 (8表)
| 表名 | 说明 |
|------|------|
| crop | 作物大类表 |
| growing_season | 生长季表 |
| growth_stage | 生长阶段表 |
| growing_season_crop | 生长季-作物关联 |
| growing_season_plot | 生长季-地块关联 |
| growing_season_growth_stage | 生长季-生长阶段关联 |
| agent_farmer_service | 服务商-农户关系 |
| farm_activity | 农事活动记录 |

### 传感器 (3表)
| 表名 | 说明 |
|------|------|
| sensor | 传感器设备表 |
| sensor_type | 传感器类型表 |
| sensor_plot | 传感器-地块绑定 |

### 气象与环境 (2表)
| 表名 | 说明 |
|------|------|
| weather_daily | 每日气象数据 |
| weather_hour | 每小时气象数据 |

### 遥感与土壤 (5表)
| 表名 | 说明 |
|------|------|
| remote_sensing_image | 遥感影像表 |
| soil_test | 土壤检测记录 |
| soil_test_templates | 土壤检测模板 |
| soil_test_items | 土壤检测项目 |
| soil_items_templates | 模板-项目关联 |

### 告警与报告 (5表)
| 表名 | 说明 |
|------|------|
| alert | 告警记录 |
| alert_level | 告警级别字典 |
| alert_type | 告警类型字典 |
| report | 分析报告表 |
| report_type | 报告类型字典 |

### 病虫害 (1表)
| 表名 | 说明 |
|------|------|
| pest_disease_type | 病虫害类型字典 |

### 农事 (1表)
| 表名 | 说明 |
|------|------|
| activity_type | 活动类型字典 |

---

## 📝 详细表结构

### 1. user 用户表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| user_id | BIGINT | NULL | 用户账号（唯一） |
| user_name | VARCHAR(100) | NULL | 用户名/登录名 |
| phone | VARCHAR(20) | NULL | 手机号码 |
| avatar_url | VARCHAR(500) | NULL | 头像URL地址 |
| gender | TINYINT | NULL | 性别：0-未知，1-男，2-女 |
| name | VARCHAR(100) | NULL | 用户真实姓名 |
| password | VARCHAR(255) | NULL | 登录密码 |
| status | TINYINT | 1 | 状态：0-禁用，1-启用 |
| created_at | DATETIME | CURRENT_TIMESTAMP | 创建时间 |

**索引**: PRIMARY KEY (id), UNIQUE KEY uk_user_id (user_id)

---

### 2. organization 组织表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| name | VARCHAR(100) | NULL | 组织名称 |
| status | TINYINT | 1 | 状态：0-禁用，1-启用 |
| phone | VARCHAR(20) | NULL | 联系电话 |
| created_at | DATETIME | CURRENT_TIMESTAMP | 创建时间 |

**索引**: PRIMARY KEY (id)

---

### 3. role 角色表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| code | VARCHAR(100) | NULL | 角色代码/编码 |
| role_name | VARCHAR(100) | NULL | 角色名称 |
| status | TINYINT | 1 | 状态：0-禁用，1-启用 |
| created_at | DATETIME | CURRENT_TIMESTAMP | 创建时间 |

**索引**: PRIMARY KEY (id)

---

### 4. permission 权限表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| code | VARCHAR(100) | NULL | 权限代码/编码 |
| name | VARCHAR(100) | NULL | 权限名称 |
| status | TINYINT | 1 | 状态：0-禁用，1-启用 |
| created_at | DATETIME | CURRENT_TIMESTAMP | 创建时间 |

**索引**: PRIMARY KEY (id)

---

### 5. user_organization 用户-组织关联表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| user_id | BIGINT | NULL | 用户ID，关联user表 |
| org_id | BIGINT | NULL | 组织ID，关联organization表 |

**索引**: PRIMARY KEY (id)

---

### 6. user_role 用户-角色关联表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| user_id | BIGINT | NULL | 用户ID，关联user表 |
| role_id | BIGINT | NULL | 角色ID，关联role表 |
| is_primary | TINYINT | 0 | 是否为主角色：0-否，1-是 |

**索引**: PRIMARY KEY (id)

---

### 7. role_permission 角色-权限关联表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| role_id | BIGINT | NULL | 角色ID，关联role表 |
| permission_id | BIGINT | NULL | 权限ID，关联permission表 |

**索引**: PRIMARY KEY (id)

---

### 8. base 基地表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| name | VARCHAR(100) | NULL | 基地名称 |
| address | VARCHAR(255) | NULL | 基地地址 |
| map | VARCHAR(500) | NULL | 地图坐标信息（经纬度或JSON） |
| status | TINYINT | 1 | 状态：0-禁用，1-启用 |
| created_at | DATETIME | CURRENT_TIMESTAMP | 创建时间 |

**索引**: PRIMARY KEY (id)

---

### 9. plot 地块表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| name | VARCHAR(100) | NULL | 地块名称 |
| area_mu | DECIMAL(10,2) | NULL | 地块面积（亩） |
| address | VARCHAR(255) | NULL | 地块地址 |
| map | VARCHAR(500) | NULL | 地图坐标信息（经纬度或JSON） |
| soil_capacity | DECIMAL(6,3) | NULL | 土壤容量（克/立方厘米） |
| status | TINYINT | 1 | 状态：0-禁用，1-启用 |
| created_at | DATETIME | CURRENT_TIMESTAMP | 创建时间 |

**索引**: PRIMARY KEY (id)

---

### 10. base_plot 基地-地块关联表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| base_id | BIGINT | NULL | 基地ID，关联base表 |
| plot_id | BIGINT | NULL | 地块ID，关联plot表 |

**索引**: PRIMARY KEY (id)

---

### 11. base_farmer 基地-农户关联表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| base_id | BIGINT | NULL | 基地ID，关联base表 |
| farmer_id | BIGINT | NULL | 农户用户ID，关联user表 |

**索引**: PRIMARY KEY (id)

---

### 12. base_agent 基地-服务商关联表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| base_id | BIGINT | NULL | 基地ID，关联base表 |
| agent_id | BIGINT | NULL | 服务商用户ID，关联user表 |

**索引**: PRIMARY KEY (id)

---

### 13. crop 作物大类表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| name | VARCHAR(100) | NULL | 作物名称 |

**索引**: PRIMARY KEY (id)

**说明**: 存储作物大类，如"水稻"、"小麦"、"玉米"等

---

### 14. growing_season 生长季表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| name | VARCHAR(100) | NULL | 生长季名称 |
| crop_name | VARCHAR(100) | NULL | 具体作物名称 |
| start_time | DATETIME | NULL | 开始时间 |
| end_time | DATETIME | NULL | 结束时间 |
| status | TINYINT | 1 | 状态：0-禁用，1-启用 |
| created_at | DATETIME | CURRENT_TIMESTAMP | 创建时间 |

**索引**: PRIMARY KEY (id)

**说明**: crop_name 存储具体作物名称（如"南粳46号"），不使用外键

---

### 15. growth_stage 生长阶段表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| name | VARCHAR(100) | NULL | 生长阶段名称 |

**索引**: PRIMARY KEY (id)

---

### 16. growing_season_crop 生长季-作物关联表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| growing_season_id | BIGINT | NULL | 生长季ID，关联growing_season表 |
| crop_id | BIGINT | NULL | 作物ID，关联crop表 |

**索引**: PRIMARY KEY (id)

---

### 17. growing_season_plot 生长季-地块关联表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| growing_season_id | BIGINT | NULL | 生长季ID，关联growing_season表 |
| plot_id | BIGINT | NULL | 地块ID，关联plot表 |

**索引**: PRIMARY KEY (id)

---

### 18. growing_season_growth_stage 生长季-生长阶段关联表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| growing_season_id | BIGINT | NULL | 生长季ID，关联growing_season表 |
| growth_stage_id | BIGINT | NULL | 生长阶段ID，关联growth_stage表 |
| start_time | DATETIME | NULL | 阶段开始时间 |
| end_time | DATETIME | NULL | 阶段结束时间 |
| status | TINYINT | 1 | 状态：0-未开始，1-进行中，2-已完成 |

**索引**: PRIMARY KEY (id)

---

### 19. agent_farmer_service 服务商-农户服务关系表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| agent_id | BIGINT | NULL | 服务商用户ID，关联user表 |
| farmer_id | BIGINT | NULL | 农户用户ID，关联user表 |

**索引**: PRIMARY KEY (id)

---

### 20. sensor 传感器设备表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| name | VARCHAR(100) | NULL | 传感器名称 |
| sensor_id | BIGINT | NULL | 传感器设备ID |
| sensor_model | VARCHAR(255) | NULL | 传感器型号 |
| status | TINYINT | 1 | 状态：0-离线，1-在线，2-故障 |

**索引**: PRIMARY KEY (id)

---

### 21. sensor_type 传感器类型表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| name | VARCHAR(100) | NULL | 传感器类型名称 |

**索引**: PRIMARY KEY (id)

---

### 22. sensor_plot 传感器-地块绑定表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| sensor_id | BIGINT | NULL | 传感器ID，关联sensor表 |
| plot_id | BIGINT | NULL | 地块ID，关联plot表 |
| status | TINYINT | 1 | 关联状态：0-未绑定，1-已绑定 |

**索引**: PRIMARY KEY (id)

---

### 23. weather_daily 每日气象数据表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| plot_id | BIGINT | NULL | 地块ID，关联plot表 |
| time | DATETIME | NULL | 日期 |
| t2m_max | DECIMAL(5,2) | NULL | 最高温度（摄氏度） |
| t2x_min | DECIMAL(5,2) | NULL | 最低温度（摄氏度） |
| tp_sum | DECIMAL(6,2) | NULL | 日降水量（毫米） |
| rh2m_avg | DECIMAL(5,2) | NULL | 平均相对湿度（%） |
| ws10m_max | DECIMAL(5,2) | NULL | 最大风速（米/秒） |
| query_date | DATETIME | NULL | 数据查询日期 |

**索引**: PRIMARY KEY (id)

---

### 24. weather_hour 每小时气象数据表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| plot_id | BIGINT | NULL | 地块ID，关联plot表 |
| time | DATETIME | NULL | 时间 |
| t2m | DECIMAL(5,2) | NULL | 温度（摄氏度） |
| tp | DECIMAL(6,2) | NULL | 降水量（毫米） |
| ws10m | DECIMAL(5,2) | NULL | 风速（米/秒） |
| rh2m | DECIMAL(5,2) | NULL | 相对湿度（%） |
| query_date | DATETIME | NULL | 数据查询日期 |
| ssrd | DECIMAL(10,2) | NULL | 太阳辐射（瓦特/平方米） |

**索引**: PRIMARY KEY (id)

---

### 25. remote_sensing_image 遥感影像表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| plot_id | BIGINT | NULL | 地块ID，关联plot表 |
| time | DATETIME | NULL | 遥感影像拍摄时间 |
| type | TINYINT | NULL | 影像类型：1-可见光，2-红外，3-多光谱 |
| image | VARCHAR(500) | NULL | 遥感影像文件路径/URL |

**索引**: PRIMARY KEY (id)

---

### 26. soil_test 土壤检测记录表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| plot_id | BIGINT | NULL | 地块ID，关联plot表 |
| templates_id | BIGINT | NULL | 检测模板ID，关联soil_test_templates表 |
| time | DATETIME | NULL | 检测时间 |
| data | JSON | NULL | 检测数据（JSON格式） |

**索引**: PRIMARY KEY (id)

---

### 27. soil_test_templates 土壤检测模板表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| name | VARCHAR(100) | NULL | 检测模板名称 |
| description | TEXT | NULL | 模板描述 |

**索引**: PRIMARY KEY (id)

---

### 28. soil_test_items 土壤检测项目表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| name | VARCHAR(100) | NULL | 土壤检测项目名称 |
| unit | VARCHAR(20) | NULL | 单位 |

**索引**: PRIMARY KEY (id)

---

### 29. soil_items_templates 模板-项目关联表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| template_id | BIGINT | NULL | 模板ID，关联soil_test_templates表 |
| item_id | BIGINT | NULL | 检测项目ID，关联soil_test_items表 |

**索引**: PRIMARY KEY (id)

---

### 30. alert 告警记录表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| plot_id | BIGINT | NULL | 地块ID，关联plot表 |
| time | DATETIME | NULL | 告警发生时间 |
| level_id | BIGINT | NULL | 告警级别ID，关联alert_level表 |
| type_id | BIGINT | NULL | 告警类型ID，关联alert_type表 |
| content | JSON | NULL | 告警内容描述 |
| status | TINYINT | 1 | 状态：0-未处理，1-已处理，2-已忽略 |

**索引**: PRIMARY KEY (id)

---

### 31. alert_level 告警级别字典表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| name | VARCHAR(100) | NULL | 告警级别名称 |

**索引**: PRIMARY KEY (id)

---

### 32. alert_type 告警类型字典表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| name | VARCHAR(100) | NULL | 告警类型名称 |

**索引**: PRIMARY KEY (id)

---

### 33. report 分析报告表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| plot_id | BIGINT | NULL | 地块ID，关联plot表 |
| time | DATETIME | NULL | 报告生成时间 |
| type_id | BIGINT | NULL | 报告类型ID，关联report_type表 |
| original_data | JSON | NULL | 原始数据（JSON格式） |
| analysis | JSON | NULL | 分析结果（JSON格式） |
| advice | JSON | NULL | 建议内容（JSON格式） |

**索引**: PRIMARY KEY (id)

---

### 34. report_type 报告类型字典表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| name | VARCHAR(100) | NULL | 报告类型名称 |

**索引**: PRIMARY KEY (id)

---

### 35. pest_disease_type 病虫害类型字典表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| name | VARCHAR(100) | NULL | 病虫害名称 |
| type | TINYINT | NULL | 类型：1-虫害，2-病害，3-草害 |

**索引**: PRIMARY KEY (id)

**说明**: 病虫害类型字典表，不关联具体地块

---

### 36. farm_activity 农事活动记录表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| plot_id | BIGINT | NULL | 地块ID，关联plot表 |
| time | DATETIME | NULL | 活动时间 |
| type_id | BIGINT | NULL | 活动类型ID，关联activity_type表 |
| topic | VARCHAR(100) | NULL | 活动主题 |
| basis | JSON | NULL | 活动依据 |
| content | JSON | NULL | 活动内容描述 |

**索引**: PRIMARY KEY (id)

---

### 37. activity_type 活动类型字典表
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | BIGINT | NOT NULL | 主键ID，自增 |
| name | VARCHAR(100) | NULL | 活动类型名称 |

**索引**: PRIMARY KEY (id)

---

## 📊 数据统计

| 数据类型 | 字段数 |
|----------|--------|
| BIGINT | 75 |
| VARCHAR | 34 |
| DATETIME | 20 |
| TINYINT | 15 |
| DECIMAL | 12 |
| JSON | 7 |
| TEXT | 1 |

---

## 📝 字段命名规范

- 主键：`id`
- 外键：`表名_id` 格式
- 时间：`xxx_at` 或 `xxx_time` 格式
- 状态：`status`
- 名称：`name`

---

## 📁 相关文件

- **SQL定义**: `sql/tables/`
- **汇总文件**: `sql/tables/init_database.sql`
- **生成脚本**: `scripts/generate_sql_with_comments.py`
