# 农田档案数据库 - 数据字典

**版本**: v2.0  
**更新时间**: 2026-03-18  
**数据库**: farmland_test  
**表数量**: 37张  
**存储引擎**: InnoDB  
**字符集**: utf8mb4_unicode_ci

---

## 目录

1. [用户权限模块](#一用户权限模块)
2. [组织基地模块](#二组织基地模块)
3. [地块管理模块](#三地块管理模块)
4. [作物管理模块](#四作物管理模块)
5. [传感器模块](#五传感器模块)
6. [气象数据模块](#六气象数据模块)
7. [遥感影像模块](#七遥感影像模块)
8. [土壤检测模块](#八土壤检测模块)
9. [告警管理模块](#九告警管理模块)
10. [报告管理模块](#十报告管理模块)
11. [农事活动模块](#十一农事活动模块)
12. [病虫害模块](#十二病虫害模块)

---

## 一、用户权限模块

### 1.1 user - 用户信息表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| user_id | bigint | NO | | UNIQUE, CHECK>0 | 用户账号（唯一） |
| user_name | varchar(100) | NO | | | 用户名/登录名 |
| phone | varchar(20) | YES | | | 手机号码 |
| avatar_url | varchar(500) | YES | | | 头像URL地址 |
| gender | tinyint | YES | | CHECK(0,1,2) | 性别：0-未知，1-男，2-女 |
| name | varchar(100) | YES | | | 用户真实姓名 |
| password | varchar(255) | YES | | | 登录密码 |
| status | tinyint | YES | 1 | CHECK(0,1) | 状态：0-禁用，1-启用 |
| created_at | datetime | YES | CURRENT_TIMESTAMP | | 创建时间 |

**索引**:
- PRIMARY KEY (`id`)
- UNIQUE KEY `uk_user_id` (`user_id`)

**CHECK约束**:
- `chk_user_gender`: gender IN (0, 1, 2)
- `chk_user_status`: status IN (0, 1)
- `chk_user_id_positive`: user_id > 0

---

### 1.2 role - 角色表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| code | varchar(100) | YES | | | 角色代码/编码 |
| role_name | varchar(100) | NO | | | 角色名称 |
| status | tinyint | YES | 1 | CHECK(0,1) | 状态：0-禁用，1-启用 |
| created_at | datetime | YES | CURRENT_TIMESTAMP | | 创建时间 |

**索引**: PRIMARY KEY (`id`)

**CHECK约束**:
- `chk_role_status`: status IN (0, 1)

---

### 1.3 permission - 权限表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| code | varchar(100) | YES | | | 权限代码/编码 |
| name | varchar(100) | NO | | | 权限名称 |
| status | tinyint | YES | 1 | CHECK(0,1) | 状态：0-禁用，1-启用 |
| created_at | datetime | YES | CURRENT_TIMESTAMP | | 创建时间 |

**索引**: PRIMARY KEY (`id`)

**CHECK约束**:
- `chk_permission_status`: status IN (0, 1)

---

### 1.4 user_role - 用户角色关联表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| user_id | bigint | NO | | FK→user.id | 用户ID，关联user表 |
| role_id | bigint | NO | | FK→role.id | 角色ID，关联role表 |
| is_primary | tinyint | YES | 0 | CHECK(0,1) | 是否为主角色：0-否，1-是 |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_user_id` (`user_id`)
- INDEX `idx_role_id` (`role_id`)

**CHECK约束**:
- `chk_user_role_is_primary`: is_primary IN (0, 1)

---

### 1.5 user_organization - 用户组织关联表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| user_id | bigint | NO | | FK→user.id | 用户ID，关联user表 |
| org_id | bigint | NO | | FK→organization.id | 组织ID，关联organization表 |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_user_id` (`user_id`)
- INDEX `idx_org_id` (`org_id`)

---

### 1.6 role_permission - 角色权限关联表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| role_id | bigint | NO | | FK→role.id | 角色ID，关联role表 |
| permission_id | bigint | NO | | FK→permission.id | 权限ID，关联permission表 |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_role_id` (`role_id`)
- INDEX `idx_permission_id` (`permission_id`)

---

### 1.7 agent_farmer_service - 服务商农户关联表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| agent_id | bigint | NO | | FK→user.id | 服务商用户ID，关联user表 |
| farmer_id | bigint | NO | | FK→user.id | 农户用户ID，关联user表 |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_agent_id` (`agent_id`)
- INDEX `idx_farmer_id` (`farmer_id`)

---

## 二、组织基地模块

### 2.1 organization - 组织表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| name | varchar(100) | NO | | | 组织名称 |
| status | tinyint | YES | 1 | CHECK(0,1) | 状态：0-禁用，1-启用 |
| phone | varchar(20) | YES | | | 联系电话 |
| created_at | datetime | YES | CURRENT_TIMESTAMP | | 创建时间 |

**索引**: PRIMARY KEY (`id`)

**CHECK约束**:
- `chk_organization_status`: status IN (0, 1)

---

### 2.2 base - 基地表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| name | varchar(100) | NO | | | 基地名称 |
| address | varchar(255) | YES | | | 基地地址 |
| map | varchar(500) | YES | | | 地图坐标信息（经纬度或JSON） |
| status | tinyint | YES | 1 | CHECK(0,1) | 状态：0-禁用，1-启用 |
| created_at | datetime | YES | CURRENT_TIMESTAMP | | 创建时间 |

**索引**: PRIMARY KEY (`id`)

**CHECK约束**:
- `chk_base_status`: status IN (0, 1)

---

### 2.3 base_agent - 基地服务商关联表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| base_id | bigint | NO | | FK→base.id | 基地ID，关联base表 |
| agent_id | bigint | NO | | FK→user.id | 服务商用户ID，关联user表 |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_base_id` (`base_id`)
- INDEX `idx_agent_id` (`agent_id`)

---

### 2.4 base_farmer - 基地农户关联表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| base_id | bigint | NO | | FK→base.id | 基地ID，关联base表 |
| farmer_id | bigint | NO | | FK→user.id | 农户用户ID，关联user表 |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_base_id` (`base_id`)
- INDEX `idx_farmer_id` (`farmer_id`)

---

### 2.5 base_plot - 基地地块关联表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| base_id | bigint | NO | | FK→base.id | 基地ID，关联base表 |
| plot_id | bigint | NO | | FK→plot.id, UNIQUE | 地块ID，关联plot表 |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_base_id` (`base_id`)
- UNIQUE KEY `uk_base_plot_plot` (`plot_id`)

---

## 三、地块管理模块

### 3.1 plot - 地块信息表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| name | varchar(100) | NO | | | 地块名称 |
| area_mu | decimal(10,2) | YES | | CHECK>0 | 地块面积（亩） |
| address | varchar(255) | YES | | | 地块地址 |
| map | varchar(500) | YES | | | 地图坐标信息（经纬度或JSON） |
| soil_capacity | decimal(6,3) | YES | | | 土壤容量（克/立方厘米） |
| status | tinyint | YES | 1 | CHECK(0,1) | 状态：0-禁用，1-启用 |
| created_at | datetime | YES | CURRENT_TIMESTAMP | | 创建时间 |

**索引**: PRIMARY KEY (`id`)

**CHECK约束**:
- `chk_plot_status`: status IN (0, 1)
- `chk_plot_area_positive`: area_mu IS NULL OR area_mu > 0

---

## 四、作物管理模块

### 4.1 crop - 作物表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| name | varchar(100) | NO | | | 作物名称 |

**索引**: PRIMARY KEY (`id`)

---

### 4.2 growing_season - 生长季表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| name | varchar(100) | NO | | | 生长季名称 |
| crop_name | varchar(100) | YES | | | 具体作物名称 |
| start_time | datetime | YES | | | 开始时间 |
| end_time | datetime | YES | | CHECK | 结束时间 |
| status | tinyint | YES | 1 | CHECK(0,1) | 状态：0-禁用，1-启用 |
| created_at | datetime | YES | CURRENT_TIMESTAMP | | 创建时间 |

**索引**: PRIMARY KEY (`id`)

**CHECK约束**:
- `chk_growing_season_status`: status IN (0, 1)
- `chk_growing_season_time`: end_time IS NULL OR end_time > start_time

---

### 4.3 growth_stage - 生长阶段表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| name | varchar(100) | NO | | | 生长阶段名称 |

**索引**: PRIMARY KEY (`id`)

---

### 4.4 growing_season_crop - 生长季作物关联表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| growing_season_id | bigint | NO | | FK→growing_season.id | 生长季ID，关联growing_season表 |
| crop_id | bigint | NO | | FK→crop.id | 作物ID，关联crop表 |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_growing_season_id` (`growing_season_id`)
- INDEX `idx_crop_id` (`crop_id`)

---

### 4.5 growing_season_plot - 生长季地块关联表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| growing_season_id | bigint | NO | | FK→growing_season.id | 生长季ID，关联growing_season表 |
| plot_id | bigint | NO | | FK→plot.id | 地块ID，关联plot表 |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_growing_season_id` (`growing_season_id`)
- INDEX `idx_plot_id` (`plot_id`)

---

### 4.6 growing_season_growth_stage - 生长季阶段关联表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| growing_season_id | bigint | NO | | FK→growing_season.id | 生长季ID，关联growing_season表 |
| growth_stage_id | bigint | NO | | FK→growth_stage.id | 生长阶段ID，关联growth_stage表 |
| start_time | datetime | YES | | | 阶段开始时间 |
| end_time | datetime | YES | | | 阶段结束时间 |
| status | tinyint | YES | 1 | CHECK(0,1,2) | 状态：0-未开始，1-进行中，2-已完成 |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_growing_season_id` (`growing_season_id`)
- INDEX `idx_growth_stage_id` (`growth_stage_id`)

**CHECK约束**:
- `chk_growing_season_growth_stage_status`: status IN (0, 1, 2)

---

## 五、传感器模块

### 5.1 sensor - 传感器设备表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| name | varchar(100) | YES | | | 传感器名称 |
| sensor_id | bigint | NO | | CHECK>0 | 传感器设备ID |
| sensor_model | varchar(255) | YES | | | 传感器型号 |
| status | tinyint | YES | 1 | CHECK(0,1,2) | 状态：0-离线，1-在线，2-故障 |

**索引**: PRIMARY KEY (`id`)

**CHECK约束**:
- `chk_sensor_status`: status IN (0, 1, 2)
- `chk_sensor_id_positive`: sensor_id > 0

---

### 5.2 sensor_type - 传感器类型表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| name | varchar(100) | NO | | | 传感器类型名称 |

**索引**: PRIMARY KEY (`id`)

---

### 5.3 sensor_plot - 传感器地块关联表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| sensor_id | bigint | NO | | FK→sensor.id | 传感器ID，关联sensor表 |
| plot_id | bigint | NO | | FK→plot.id | 地块ID，关联plot表 |
| status | tinyint | YES | 1 | CHECK(0,1) | 关联状态：0-未绑定，1-已绑定 |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_sensor_id` (`sensor_id`)
- INDEX `idx_plot_id` (`plot_id`)

**CHECK约束**:
- `chk_sensor_plot_status`: status IN (0, 1)

---

## 六、气象数据模块

### 6.1 weather_daily - 每日气象数据表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| plot_id | bigint | NO | | FK→plot.id | 地块ID，关联plot表 |
| time | datetime | NO | | | 日期（每日数据） |
| t2m_max | decimal(5,2) | YES | | | 最高温度（摄氏度） |
| t2x_min | decimal(5,2) | YES | | | 最低温度（摄氏度） |
| tp_sum | decimal(6,2) | YES | | | 日降水量（毫米） |
| rh2m_avg | decimal(5,2) | YES | | | 平均相对湿度（%） |
| ws10m_max | decimal(5,2) | YES | | | 最大风速（米/秒） |
| query_date | datetime | YES | | | 数据查询日期 |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_plot_id` (`plot_id`)
- INDEX `idx_plot_id_time` (`plot_id`, `time`)

---

### 6.2 weather_hour - 每小时气象数据表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| plot_id | bigint | NO | | FK→plot.id | 地块ID，关联plot表 |
| time | datetime | NO | | | 时间（每小时数据） |
| t2m | decimal(5,2) | YES | | | 温度（摄氏度） |
| tp | decimal(6,2) | YES | | | 降水量（毫米） |
| ws10m | decimal(5,2) | YES | | | 风速（米/秒） |
| rh2m | decimal(5,2) | YES | | | 相对湿度（%） |
| query_date | datetime | YES | | | 数据查询日期 |
| ssrd | decimal(10,2) | YES | | | 太阳辐射（瓦特/平方米） |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_plot_id` (`plot_id`)
- INDEX `idx_plot_id_time` (`plot_id`, `time`)

---

## 七、遥感影像模块

### 7.1 remote_sensing_image - 遥感影像表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| plot_id | bigint | NO | | FK→plot.id | 地块ID，关联plot表 |
| time | datetime | NO | | | 遥感影像拍摄时间 |
| type | tinyint | YES | | CHECK(1,2,3) | 影像类型：1-可见光，2-红外，3-多光谱 |
| image | varchar(500) | YES | | | 遥感影像文件路径/URL |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_plot_id` (`plot_id`)
- INDEX `idx_plot_id_time` (`plot_id`, `time`)

**CHECK约束**:
- `chk_remote_sensing_image_type`: type IN (1, 2, 3)

---

## 八、土壤检测模块

### 8.1 soil_test - 土壤检测表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| plot_id | bigint | NO | | FK→plot.id | 地块ID，关联plot表 |
| templates_id | bigint | YES | | FK→soil_test_templates.id | 检测模板ID，关联soil_test_templates表 |
| time | datetime | NO | | | 检测时间 |
| data | json | YES | | | 检测数据（JSON格式） |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_plot_id` (`plot_id`)
- INDEX `idx_plot_id_time` (`plot_id`, `time`)
- INDEX `idx_templates_id` (`templates_id`)

---

### 8.2 soil_test_items - 土壤检测项目表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| name | varchar(100) | NO | | | 土壤检测项目名称 |
| unit | varchar(20) | YES | | | 单位 |

**索引**: PRIMARY KEY (`id`)

---

### 8.3 soil_test_templates - 土壤检测模板表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| name | varchar(100) | NO | | | 检测模板名称 |
| description | text | YES | | | 模板描述 |

**索引**: PRIMARY KEY (`id`)

---

### 8.4 soil_items_templates - 检测项目模板关联表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| template_id | bigint | NO | | FK→soil_test_templates.id | 模板ID，关联soil_test_templates表 |
| item_id | bigint | NO | | FK→soil_test_items.id | 检测项目ID，关联soil_test_items表 |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_template_id` (`template_id`)
- INDEX `idx_item_id` (`item_id`)

---

## 九、告警管理模块

### 9.1 alert - 告警表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| plot_id | bigint | NO | | FK→plot.id | 地块ID，关联plot表 |
| time | datetime | NO | | | 告警发生时间 |
| level_id | bigint | NO | | FK→alert_level.id | 告警级别ID，关联alert_level表 |
| type_id | bigint | NO | | FK→alert_type.id | 告警类型ID，关联alert_type表 |
| content | json | YES | | | 告警内容描述 |
| status | tinyint | YES | 1 | CHECK(0,1,2) | 状态：0-未处理，1-已处理，2-已忽略 |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_plot_id` (`plot_id`)
- INDEX `idx_plot_id_status` (`plot_id`, `status`)
- INDEX `idx_plot_id_time` (`plot_id`, `time`)
- INDEX `idx_level_id` (`level_id`)
- INDEX `idx_type_id` (`type_id`)

**CHECK约束**:
- `chk_alert_status`: status IN (0, 1, 2)

---

### 9.2 alert_level - 告警级别表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| name | varchar(100) | NO | | | 告警级别名称 |

**索引**: PRIMARY KEY (`id`)

---

### 9.3 alert_type - 告警类型表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| name | varchar(100) | NO | | | 告警类型名称 |

**索引**: PRIMARY KEY (`id`)

---

## 十、报告管理模块

### 10.1 report - 报告表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| plot_id | bigint | NO | | FK→plot.id | 地块ID，关联plot表 |
| time | datetime | NO | | | 报告生成时间 |
| type_id | bigint | NO | | FK→report_type.id | 报告类型ID，关联report_type表 |
| original_data | json | YES | | | 原始数据（JSON格式） |
| analysis | json | YES | | | 分析结果（JSON格式） |
| advice | json | YES | | | 建议内容（JSON格式） |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_plot_id` (`plot_id`)
- INDEX `idx_plot_id_time` (`plot_id`, `time`)
- INDEX `idx_type_id` (`type_id`)

---

### 10.2 report_type - 报告类型表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| name | varchar(100) | NO | | | 报告类型名称 |

**索引**: PRIMARY KEY (`id`)

---

## 十一、农事活动模块

### 11.1 farm_activity - 农事活动表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| plot_id | bigint | NO | | FK→plot.id | 地块ID，关联plot表 |
| time | datetime | NO | | | 活动时间 |
| type_id | bigint | NO | | FK→activity_type.id | 活动类型ID，关联activity_type表 |
| topic | varchar(100) | YES | | | 活动主题 |
| basis | json | YES | | | 活动依据 |
| content | json | YES | | | 活动内容描述 |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_plot_id` (`plot_id`)
- INDEX `idx_plot_id_time` (`plot_id`, `time`)
- INDEX `idx_type_id` (`type_id`)

---

### 11.2 activity_type - 活动类型表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| name | varchar(100) | NO | | | 活动类型名称 |

**索引**: PRIMARY KEY (`id`)

---

## 十二、病虫害模块

### 12.1 pest_disease_type - 病虫害类型字典表

| 字段名 | 类型 | 允许空 | 默认值 | 约束 | 说明 |
|--------|------|--------|--------|------|------|
| id | bigint | NO | | PRIMARY | 主键ID，自增 |
| name | varchar(100) | NO | | | 病虫害类型名称 |
| type | tinyint | NO | | CHECK(1,2,3) | 类型：1-虫害，2-病害，3-草害 |

**索引**: PRIMARY KEY (`id`)

**CHECK约束**:
- `chk_pest_disease_type_type`: type IN (1, 2, 3)

---

## 附录

### A. 索引统计

| 类型 | 数量 |
|------|------|
| 主键索引 | 37 |
| 唯一索引 | 2 |
| 普通索引 | 42 |
| 联合索引 | 14 |
| **总计** | **95** |

### B. 外键统计

| 模块 | 数量 |
|------|------|
| 用户权限 | 7 |
| 组织基地 | 8 |
| 作物管理 | 7 |
| 传感器 | 3 |
| 气象数据 | 2 |
| 遥感影像 | 1 |
| 土壤检测 | 4 |
| 告警管理 | 3 |
| 报告管理 | 2 |
| 农事活动 | 2 |
| **总计** | **35** |

### C. CHECK约束统计

| 表名 | 约束数量 |
|------|----------|
| user | 3 |
| growing_season | 2 |
| sensor | 2 |
| plot | 2 |
| 其他表 | 各1 |
| **总计** | **18** |

---

*数据字典版本: v2.0*  
*最后更新: 2026-03-18*
